// ── Discord ───────────────────────────────────────────────────
export const ADMINISTRATOR_PERMISSION = 0x8;

// ── XP system ─────────────────────────────────────────────────
export const VOICE_XP_INTERVAL_MS = 60_000; // 1 minute

// ── Leaderboard ───────────────────────────────────────────────
export const LEADERBOARD_PAGE_SIZE = 10;
export const COLLECTOR_TIMEOUT_MS = 120_000; // 2 minutes

// ── TagScript limits ──────────────────────────────────────────
export const MAX_TEMPLATE_LENGTH = 2_000;
export const MAX_OUTPUT_LENGTH = 4_000;
export const MAX_ITERATIONS = 10;
export const MAX_MATH_EXPR_LENGTH = 100;
export const MAX_MATH_DEPTH = 20;

// ── Default messages ──────────────────────────────────────────
export const DEFAULT_LEVEL_UP_MESSAGE =
    '🎉 {mention} a atteint le niveau {level} ! Félicitations !';

